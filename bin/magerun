#!/bin/bash
project=$(pwd | sed "s|$HOME/Sites/||" | cut -d'/' -f1)

if docker ps --format '{{.Names}}' | grep -q "^php-${project}$"; then
    container="php-$project"
else
    container="php"
fi

docker exec -uapp -i -w "/var/www/$project" "$container" magerun "$@"
